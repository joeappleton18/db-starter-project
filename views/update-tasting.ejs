<!DOCTYPE html>
<html lang="en">
  <html lang="en">
    <head>
      <meta charset="utf-8" />
      <meta
        name="viewport"
        content="width=device-width, initial-scale=1, shrink-to-fit=no"
      />
      <meta name="description" content="" />
      <title>World Wines</title>

      <link
        rel="canonical"
        href="https://getbootstrap.com/docs/4.5/examples/album/"
      />

      <!-- Bootstrap core CSS -->
      <link
        rel="stylesheet"
        href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
        crossorigin="anonymous"
      />

      <link href="css/album.css" rel="stylesheet" />
    </head>
    <body>
      <%- include('common/header'); -%>

      <div class="container">
        <h1 class="text-center">Create A New Wine Tasting 🍷</h1>
        <div class="row justify-content-md-center">
          <div class="col-">
            <form method="POST" action="/create-tasting">
              <div class="form-group">
                <label for="name">Wine Title </label>
                <input
                  value="<%= tasting.title %>" 
                  type="text"
                  class="form-control"
                  id="name"
                  name="title"
                  placeholder="Enter Title"
                />

                <% if (errors['title']) { %>
                <p style="color: red"><%= errors['title'].message %></p>
                <% } %>
              </div>

              <div class="form-group">
                <label for="name">Points</label>
                <input
                  value="<%= tasting.points %>" 
                  type="number"
                  class="form-control"
                  id="name"
                  name="points"
                />

                <% if (errors['points']) { %>
                <p style="color: red"><%= errors['points'].message %></p>
                <% } %>
              </div>

              <div class="form-group">
                <label for="exampleFormControlSelect1">Select Taster</label>
                <select
                 value="<%= tasting.taster_id %>" 
                  class="form-control"
                  name="taster_id"
                  id="exampleFormControlSelect1"
                >
                  <% tasters.forEach( taster => { %>

                  <option value="<%= taster._id %>"><%= taster.name %></option>

                  <% }) %>
                </select>
              </div>

             

              <div class="form-group">
   
                
                <% regions.forEach( region => { %>
               <div class="form-check form-check-inline">
                <input
                  name="regions"
                  class="form-check-input"
                  type="checkbox"
                  <% if (tasting.regions.includes(region._id)) {%>   checked <% } %> 
                  id="inlineCheckbox2"
                  value= "<%= region._id %>"
                />
                <label class="form-check-label" for="inlineCheckbox2"><%= region.name %></label>
              </div>

            <% }) %>


             
              <button type="submit" class="btn btn-primary">Submit</button>
            </form>
          </div>
        </div>
      </div>
      <%- include('common/footer'); -%>
    </body>
  </html>
</html>
